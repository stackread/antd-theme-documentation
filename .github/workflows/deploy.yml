# Build and deploy the docs to GitHub Pages.
#
# Installs from the npm registry - the @stackread/* packages must be
# published for this to be green. Pages source must be set to
# "GitHub Actions" once, in the repo settings.
name: deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
        env:
          DOCS_URL: https://stackread.github.io
          DOCS_BASE_URL: /antd-theme-documentation/
      - uses: actions/configure-pages@v5
        with:
          # Enables Pages on first run - no manual settings switch needed.
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: build
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
