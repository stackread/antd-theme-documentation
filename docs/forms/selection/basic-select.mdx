---
title: Basic select
description: Ant Design Select
antd_components: [Select]
sidebar_position: 1
---

import FormRowCallout from '../_partials/_form-row-callout.mdx';
import FormUsage from '../_partials/_form-usage.mdx';

# Basic select

A dropdown for choosing one option from a list.

Use it for something like 7 to 12 options. Below that a radio group shows all
the choices at once and costs a click less; far above it, the reader is
scrolling a list they cannot search, and a combo box is the better answer.

```tsx interactive
import React from 'react';
import { Select, Space, Switch } from 'antd';

export default () => {
  const [compressed, setCompressed] = React.useState(false);
  const [disabled, setDisabled] = React.useState(false);
  const [invalid, setInvalid] = React.useState(false);
  const [loading, setLoading] = React.useState(false);

  const toggles = [
    ['Compressed', compressed, setCompressed],
    ['Disabled', disabled, setDisabled],
    ['Invalid', invalid, setInvalid],
    ['Loading', loading, setLoading],
  ];

  return (
    <Space orientation="vertical" size={16} style={{ display: 'flex', maxInlineSize: 400 }}>
      <Space size={16} wrap>
        {toggles.map(([name, value, setIt]) => (
          <label key={name} style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
            <Switch size="small" checked={value} onChange={setIt} />
            {name}
          </label>
        ))}
      </Space>

      <Select
        aria-label="A basic select"
        defaultValue="warning"
        size={compressed ? 'small' : 'middle'}
        disabled={disabled}
        status={invalid ? 'error' : undefined}
        loading={loading}
        style={{ inlineSize: '100%' }}
        options={[
          { value: 'warning', label: 'Warning' },
          { value: 'minor', label: 'Minor' },
          { value: 'major', label: 'Major' },
          { value: 'critical', label: 'Critical' },
        ]}
      />
    </Space>
  );
};
```

<FormRowCallout />

<FormUsage />
